<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Education Portal</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #3a7bd5, #00d2ff);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        
        .container {
            text-align: center;
            margin-bottom: 40px;
            max-width: 1200px;
        }
        
        .title {
            color: white;
            font-size: 3rem;
            margin-bottom: 10px;
            text-shadow: 0 2px 4px rgba(0,0,0,0.2);
            letter-spacing: 1px;
        }
        
        .subtitle {
            color: rgba(255,255,255,0.9);
            font-size: 1.3rem;
            font-weight: 300;
            margin-bottom: 50px;
            text-shadow: 0 1px 2px rgba(0,0,0,0.1);
        }
        
        .main-box {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 40px;
            margin: 0 auto;
        }
        
        .box {
            background-color: white;
            padding: 35px 30px;
            border-radius: 16px;
            box-shadow: 0 15px 30px rgba(0,0,0,0.1);
            width: 300px;
            transition: all 0.4s ease;
            cursor: pointer;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        
        .box::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: linear-gradient(to right, #3a7bd5, #00d2ff);
            transform: scaleX(0);
            transition: transform 0.4s ease;
            transform-origin: left;
        }
        
        .box:hover::before {
            transform: scaleX(1);
        }
        
        .box:hover {
            transform: translateY(-15px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.2);
        }
        
        .box i {
            font-size: 60px;
            color: #3a7bd5;
            margin-bottom: 25px;
            transition: transform 0.3s ease;
        }
        
        .box:hover i {
            transform: scale(1.1);
        }
        
        .box h3 {
            font-size: 1.7rem;
            margin-bottom: 20px;
            color: #333;
            font-weight: 500;
        }
        
        .box p {
            color: #666;
            font-size: 1rem;
            line-height: 1.6;
        }
        
        a {
            text-decoration: none;
            color: inherit;
        }
        
        .footer {
            color: rgba(255,255,255,0.8);
            text-align: center;
            padding: 20px;
            margin-top: auto;
            width: 100%;
            font-size: 0.95rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="title">Education Portal</h1>
        <p class="subtitle">Select your role to access the system</p>
        
        <div class="main-box">
            <a href="/login">
                <div class="box">
                    <i class="fa-solid fa-user-shield"></i>
                    <h3>Admin</h3>
                    <p>Manage users, courses, and system settings with administrative privileges.</p>
                </div>
            </a>
            
            <a href="/login">
                <div class="box">
                    <i class="fa-solid fa-chalkboard-user"></i>
                    <h3>Teacher</h3>
                    <p>Create and manage courses, assignments, and track student progress.</p>
                </div>
            </a>
            
            <a href="/login">
                <div class="box">
                    <i class="fa-solid fa-graduation-cap"></i>
                    <h3>Student</h3>
                    <p>Access course materials, submit assignments, and track your learning journey.</p>
                </div>
            </a>
        </div>
    </div>
    
    <div class="footer">
        © 2025 Education Portal | All Rights Reserved
    </div>
</body>
</html>